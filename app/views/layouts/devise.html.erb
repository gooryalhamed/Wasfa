<!DOCTYPE html>
<html>
<head>
	<title>Wasfa - User Authentication</title>
	<%= csrf_meta_tags %>
	<%= stylesheet_link_tag 'devise', media: 'all', 'data-turbolinks-track': 'true' %>
	<%= javascript_include_tag 'application' %>
</head>
<body>
	<div class="container">
		<div class="row">
			<div class="col-xs-12">
				<%= yield %>
			</div>
		</div>
	</div>
</body>
</html>